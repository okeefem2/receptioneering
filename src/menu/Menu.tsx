import React from 'react'
import { observer } from 'mobx-react';
import './Menu.scss';

export const Menu: React.FC = observer(() => {
    return (
        <section id="menu">
            <h2>Menu</h2>
        </section>
    );
});

export default Menu;
